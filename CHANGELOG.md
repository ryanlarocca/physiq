# Physiq — CHANGELOG

Append-only history of ships for the Physiq weight + macro PWA (https://ryanlarocca.github.io/physiq/). Current-state view lives in [STATUS.md](./STATUS.md).

---

## Ship index (condensed)

### Recent ships (newest first)

- **July 1, 2026** — **Gym Tracker ported INTO the app (`index.html`), gated to Ryan only via a user-id allowlist.** Moved the tracker out of the Mission Control prototype and into the real PWA so Ryan can do critical testing on his phone in the actual app — **without touching anything for other users.** **Gating:** a hardcoded `GYM_BETA_USERS` allowlist (Ryan's id `59933c83…`); `setupGymTab()` (called from `loadAppData`) reveals the 🏋️ nav button + preloads data ONLY for allowlisted users. Everyone else sees zero change — no tab, no extra network calls. **Safe by construction:** even if someone un-hid the tab in devtools, RLS (`auth.uid()=user_id`) + the fact that `anon` has NO privileges (only `authenticated` was granted CRUD) means they'd see their own empty tracker, never anyone's data. **Vanilla-JS port** (the prototype was React): new `#view-gym` + `renderGymView()` reading `gym_exercises`/`gym_sets`/`gym_cardio` directly through `sbFetch` (user JWT → RLS auto-scopes rows); bodyweight for stats reuses the already-loaded `_weightCache`. **Feature parity with the prototype:** strength cards (last set · 🏆 PR · e1RM sparkline) → log sheet w/ in-field voice · **top voice quick-log** ("bench press 225 for 5" → fuzzy-match → confirm → log) · Cardio tab · **Stats** (DOTS score + relative-strength tiers + strength-vs-bodyweight overlay via Chart.js, in the app's palette). SW `v8→v9`. **Verified:** new headless `scripts/test-gym.mjs` — **11/11** (tab hidden for a non-allowlisted real session + no JS errors; then allowlisted → cards/PR/stats/chart all render reading own data via RLS from the browser). **Deployed:** GitHub Pages from `main`. **To promote a new tester later:** add their user id to `GYM_BETA_USERS` (or graduate to a `user_features` table for no-redeploy toggles).

- **June 30, 2026** — **Gym Tracker prototype (strength/cardio logging + relative-strength stats + voice) — prototype-first, built in Mission Control.** New Physiq feature: a real workout tracker to replace the hardcoded mock workout log. **Prototype-first per Ryan:** built standalone at `mission-control` `/physiq/gym-tracker` (private to Ryan — no Physiq user can load Mission Control), to live with before it goes into the user-facing PWA. **Schema** (new tables in the Physiq Supabase project `msmlqdrfsieixudgsced`): `gym_exercises`, `gym_sets` (numeric reps for "8.25"), `gym_cardio` — all with `user_id` + RLS `auth.uid()=user_id`, migration `mission-control/supabase/migrations/2026-06-30_gym_tracker.sql`. **Seeded** Ryan's real lifts (Dec '25→Jun '26, 10 exercises / 72 sets) under his real user id `59933c83…`, with `is_pr` recomputed from Epley **e1RM** = `weight*(1+reps/30)` (correctly ranks Squat 325×8 over the hand-marked 345×5). **Data flows server-side** through `mission-control` API routes (`/api/physiq/gym/{strength,exercises,cardio,stats}`) using the **service_role** key — a pure-client anon read returns zero rows under RLS with no auth session. **NOTE / reverses an earlier gotcha:** granted `service_role` table privileges on the gym tables and **read-only SELECT on `weight_entries` + `macro_entries`** so the stats route can join strength with bodyweight (previously service_role REST was ungranted on these tables). **UI:** Strength tab (exercise cards w/ last set · 🏆 PR · e1RM sparkline → bottom-sheet log w/ in-card Web-Speech voice + PR toast) · Cardio tab · **top-of-page voice quick-log** ("bench press 225 for 5" → fuzzy-match exercise → confirm chip → log; never auto-submits) · **Stats tab** — **DOTS** "Strength Score" (322, Intermediate) + **relative-strength tiers** (×bodyweight vs Beginner→Elite standards: Squat 2.19× Advanced, DL 2.06× Int, Bench 1.43× Int) + **strength-vs-bodyweight overlay** (recomp view, joins the 267-day weight log). **Next:** Ryan lives with it / debugs, then port the Workout tab into physiq-app (`index.html`) behind a **per-user feature flag** (user-id allowlist → `user_features` table) so it's just-Ryan first, then testers. **Verified:** `tsc --noEmit` + `next build` clean; live on :3001 (cards, PRs, stats payload, write path all confirmed). **NOT deployed** to Vercel/Pages; the existing Physiq app + `PhysiqWidget.tsx` are untouched. **Infra learned:** running `next dev` in the mission-control repo clobbers the launchd `next start` production build serving :3001 → crash loop; recover with `next build` + `launchctl kickstart -k gui/$(id -u)/com.lrghomes.mission-control` (saved to agent memory `feedback_mc_next_dev_clobbers_prod`).

- **June 29, 2026 (pt 3)** — **Telegram login alerts + reliable OTP email (Gmail SMTP); custom domain explored & deferred.** Ryan wanted (a) a ping when anyone logs into Physiq and (b) a prettier URL. **Login alerts:** added a gated `physiq-api/api/notify-login` endpoint (`verifyUser` → Telegram `sendMessage` via the existing alerts bot `8230494896` → Ryan's chat); the app fires it from the `SIGNED_IN` handler (non-blocking — never delays/breaks login). The verified token's email is reported (unspoofable). Shared CORS helper added (`api/_cors.js`, allowlists github.io + the future domain). **Tested end-to-end 4/4** (real OTP login via the UI → `notify-login` called with bearer → real Telegram alert delivered). **Email — important find:** OTP delivery was on Supabase's **built-in email, capped at 2/hr** (and exhausted just by testing) — it would have silently failed for real users. Configured **custom SMTP via Gmail** (`smtp.gmail.com:587`, app password for `ryan@lrghomes.com`, sender `ryan@lrghomes.com`) through the Management API; raised `rate_limit_email_sent` 2→30/hr. Verified: direct SMTP auth+send, Supabase OTP send `200`, and the full real-login flow. (Resend was set up first — API key + `send.lrghomes.com` registered — but dropped for now because its domain verification needs DNS; parked for later scaling.) **Custom domain `physiq.lrghomes.com`:** prepped everything (manifest `start_url`/`scope` → relative, SW precache → relative — which also **fixed the SW root-path 404 bug**; SW `v7→v8`; proxy CORS), but **deferred** — `lrghomes.com` DNS lives in Google Cloud DNS (no API creds + Ryan didn't want to deal with it), and a dedicated Vercel-managed domain was offered but declined. **App stays at `ryanlarocca.github.io/physiq/`.** New scripts: `users-report.mjs` (who's signed up + how active, from existing data), `set-smtp.mjs`, `test-login-alert.mjs`. **Deployed:** proxy via Vercel; app via GitHub Pages (`v8`). **Commits:** `25a3b69` + this.

- **June 29, 2026 (pt 2)** — **In-app "Install Physiq" helper (Add to Home Screen).** Ryan wanted users to install the PWA without him explaining the iOS steps. The app was already a valid PWA (manifest `display:standalone` + apple meta tags), but iOS Safari has **no install API** (no `beforeinstallprompt`), so you can't trigger it from a button — only guide the user. **Built** a dismissible bottom banner ([`index.html`](./index.html)) that adapts per platform: **iOS Safari** → "Add" button opens an instructions sheet (Share-icon SVG → "Add to Home Screen"), with a note to open in Safari if in an in-app webview; **Android/Chromium** → captures `beforeinstallprompt` for a real **one-tap Install**; **already-installed** (`display-mode:standalone` / `navigator.standalone`) or previously-dismissed → never shows. SW `v6→v7`. **Tests:** `scripts/test-install.mjs` 9/9 (puppeteer, faithful per-platform sim — suppresses Chromium's native event so the iOS path tests like real Safari); UI smoke still 12/12. **Deployed:** GitHub Pages from `main`.

- **June 29, 2026** — **Passwordless OTP login + true per-user data + AI moved to Anthropic directly (multi-user launch).** Ryan wanted new users to sign up via the shared link and get their own isolated data; testers had reported "got the email but couldn't log in." **Root causes found:** (1) signup used password + email **confirmation**, and the confirmation link pointed at a dead-end `confirm-email.html` that threw away the token — plus PKCE breaks across in-app webviews; the DB showed **3 external users stuck `confirmed:false`**. (2) `seedFromDataJson()` injected Ryan's bundled `data.json` into **every empty account**, so new users got Ryan's weigh-ins/macros — the real "not separate data" bug (caught when a headless test user got seeded with Ryan's 186.2 weights). (3) AI food logging was already broken in prod — the `OPENROUTER_API_KEY` was dead ("User not found" from OpenRouter). **Fixes:** replaced password UI with **email OTP** (`signInWithOtp`→`verifyOtp`, 6-digit code typed back in the same tab — no link, works in any browser); one flow for signup+login; stuck users auto-recover by requesting a code. Configured Supabase OTP email template (`{{ .Token }}`, 6-digit) via the Management API. **Removed** `seedFromDataJson` (new users start empty) and the cross-user `migrateUnownedData` landmine. Switched both `parse-meal` (text) and `parse-meal-photo` (vision) from OpenRouter → **Anthropic Messages API** (`claude-sonnet-4-6`), set `ANTHROPIC_API_KEY` in Vercel. **JWT-gated** all 3 `physiq-api` endpoints (`verifyUser` against `/auth/v1/user`) so only signed-in users spend AI; client now sends the bearer token. Deleted the dead in-app Gemini-key field + `confirm-email.html`; bug reports now carry the real logged-in email; SW `v5→v6`. **Tests (all green):** RLS isolation 10/10 (reads/writes/spoof-blocked/anon-blocked, real user JWTs), proxy gating 4/4, headless UI 12/12 (OTP screen, app boot, **new-user-starts-empty**), live text+photo parse 200. Full Supabase CSV backup taken pre-ship (`backups/`, gitignored); test pollution cleaned, Ryan's data verified intact (265 weight / 723 macro). **Files:** [`index.html`](./index.html), [`sw.js`](./sw.js), `physiq-api/api/{parse-meal,parse-meal-photo,submit-bug,_auth}.js`, new `scripts/`. **Deployed:** proxy via Vercel (Anthropic + gated); app via GitHub Pages from `main`.

- **June 15, 2026** — **Weight chart Y-axis now zooms to actual weights — the goal line was stretching the chart.** Ryan flagged that the weight chart looked too "flat" — current weights cluster ~191–196 but the chart bottom was anchored down at 171 because the **goal weight of 175** was being treated as a data point, pulling `yMin` down and burning ~20 lbs of vertical space on a region with no data. **Root cause:** Chart.js's default auto-scaling considers every dataset (weights, 7-day avg, 30-day avg, and the horizontal goal line) when computing `min`/`max`, then adds its own padding — so a goal point well outside the actual weight cluster anchors the axis to itself. **Fix:** compute `yMin` / `yMax` manually from **visible weights + the 7-day moving average only** (goal explicitly excluded from the bounds calculation), apply a 15% buffer, `Math.floor` / `Math.ceil` to clean integers, and wire those into the y-axis scale config. The goal line still renders on the chart; it just no longer drags the axis around. **Files:** [`index.html`](./index.html) — bounds computation at ~lines 2840–2848, y-axis scale config a few lines below. **Commits:** `dd9cdf7` "Weight chart: dynamic Y-axis scaling to actual data range" + follow-up `0d34c6b` "fix(chart): exclude goal from Y-axis bounds so chart zooms to actual weights" (first pass still included goal in bounds; second pass fixed it). **Verified:** chart on live site now zooms tight to the 191–196 cluster with a small buffer; goal line at 175 still visible; works across 7D / 14D / 30D / All ranges. **Deployed:** GitHub Pages auto-deploy from `main`.
